apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-web-cluster
spec:
  selector:
    app: {{ .Release.Name }}-web
  ports:
    - name: web
      protocol: TCP
      port: 9001
      targetPort: {{ .Values.web.port }}
  type: ClusterIP